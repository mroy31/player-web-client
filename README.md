# player-web-client

`player-web-client` is a client for [go-video-daemon](https://github.com/mroy31/go-video-daemon)

To build the docker image:

- Change the URL to access to your container in the script `build.sh`
- Run the script `build.sh`

